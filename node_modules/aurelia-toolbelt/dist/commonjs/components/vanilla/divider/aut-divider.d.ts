import { SharedIndex } from './../../../utilities/vanilla/sharedIndex';
export declare class DividerCustomElement {
    private element;
    private sharedIndex;
    id: string;
    class: string;
    vertical: boolean | string;
    color: string;
    backgroundColor: string;
    lineColor: string;
    private divider;
    constructor(element: Element, sharedIndex: SharedIndex);
    private attached();
}
