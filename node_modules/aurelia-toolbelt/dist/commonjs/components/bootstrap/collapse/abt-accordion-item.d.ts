export declare class BootstrapAccordionItem {
    private element;
    class: string;
    style: string;
    title: string;
    active: boolean;
    private collapse;
    constructor(element: Element);
    private onAnchorClick(event);
}
