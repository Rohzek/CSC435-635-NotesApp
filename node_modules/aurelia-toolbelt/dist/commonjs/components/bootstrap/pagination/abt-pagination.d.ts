export declare class BootstrapPaginationCustomElement {
    totalPages: number | string;
    selectedPage: number | string;
    visiblePages: number | string;
    hideOnlyOnePage: boolean | string;
    boundaryLinks: boolean | string;
    directionLinks: boolean | string;
    showGoto: boolean | string;
    template: string;
    style: string;
    class: string;
    id: string;
    size: string;
    firstText: string;
    firstIcon: string;
    lastText: string;
    lastIcon: string;
    prevText: string;
    prevIcon: string;
    nextText: string;
    nextIcon: string;
    loop: boolean | string;
    pageChanged: Function;
    private showNumbers;
    private pagination;
    private paginationTemplate;
    private pages;
    private createInput(value, min, max, height);
    private onClick(event, selectedPageNumber, prevPageNumber, nextPageNumber);
    private createVisibleItems(visibleItem, selectedItem, totalPages);
    private showLeftDots(selectedItem);
    private showRightDots(selectedItem, totalPages);
    private afterAttached();
}
