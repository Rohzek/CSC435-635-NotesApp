import { SharedIndex } from './../../../utilities/vanilla/sharedIndex';
export declare class CarouselItemCustomElement {
    private element;
    private sharedController;
    active: boolean | string;
    private isActive;
    private carouselItem;
    private carouselItemTemplate;
    private times;
    constructor(element: Element, sharedController: SharedIndex);
    private createIndicatorHtml(id, index, beActive);
    private afterAttached();
}
