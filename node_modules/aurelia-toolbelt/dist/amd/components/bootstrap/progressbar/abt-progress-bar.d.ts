export declare type ColorType = 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'light' | 'dark';
export declare class BootstrapProgressBar {
    type: ColorType;
    color: string;
    gradientColor: string;
    style: string;
    class: string;
    value: number | string;
    min: number | string;
    max: number | string;
    animated: boolean | string;
    striped: boolean | string;
    private isAnimated;
    private isStriped;
    private progressbar;
    private progressbarTemplate;
    private afterAttached();
    private gradientColorChanged(newValue);
}
