import { BindingEngine } from 'aurelia-framework';
import 'aureliatoolbelt-thirdparty/bootstrap-toggle/bootstrap-toggle.js';
export declare class BootstrapToggleCustomElement {
    private element;
    private bindingEngine;
    private on;
    private off;
    private onType;
    private offType;
    private css;
    private size;
    private width;
    private height;
    private class;
    private style;
    value: any;
    model: any;
    checked: any;
    matcher: Function;
    disabled: boolean | string;
    rtl: boolean | string;
    private checkbox;
    private subscription;
    constructor(element: Element, bindingEngine: BindingEngine);
    private disabledChanged(newValue);
    private onChanged();
    private synchronizeModel(newState);
    private checkedChanged(newValue);
    private synchronizeView(newValue);
    private attached();
    private bind();
    private unbind();
    private disposeSubscription();
}
