import { SharedIndex } from './../../../utilities/vanilla/sharedIndex';
import { Uuid } from '../../../utilities/vanilla/uuid';
export declare class IdCustomAttribute {
    private element;
    private sharedIndex;
    private idgeneratorV4;
    private id;
    constructor(element: Element, sharedIndex: SharedIndex, idgeneratorV4: Uuid);
    private bind();
}
