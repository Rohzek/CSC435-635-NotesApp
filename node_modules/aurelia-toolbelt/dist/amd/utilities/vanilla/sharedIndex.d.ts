export declare class SharedIndex {
    private dictionary;
    getAndIncrement(id: string): any;
    getValue(id: string): any;
    reset(id: string): void;
    resetAll(): void;
    private getId(id);
}
