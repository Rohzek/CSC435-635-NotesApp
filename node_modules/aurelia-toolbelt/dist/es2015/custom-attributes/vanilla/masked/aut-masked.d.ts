export declare class MaskedInputCustomAttribute {
    private element;
    mask: String;
    regex: String | RegExp;
    constructor(element: Element);
    private maskChanged(newMask);
    private regexChanged(newRegex);
}
