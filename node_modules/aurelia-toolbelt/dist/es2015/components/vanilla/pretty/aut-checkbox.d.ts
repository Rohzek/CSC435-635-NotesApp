import { BindingEngine } from 'aurelia-framework';
export declare class PrettyCheckboxCustomElement {
    private element;
    private bindingEngine;
    value: any;
    model: any;
    checked: any;
    matcher: any;
    disabled: boolean | string;
    name: string;
    switch: boolean | String;
    outlined: boolean | String;
    color: string;
    offColor: string;
    offLabel: string;
    animation: string;
    private thickCss;
    private plainCss;
    private colorCss;
    private offColorCss;
    private appearanceCSS;
    private animationCss;
    private isCheckBox;
    private state;
    private subscription;
    constructor(element: Element, bindingEngine: BindingEngine);
    private changed();
    private synchronizeModel(newState);
    private checkedChanged(newValue);
    private synchronizeView(newValue);
    private disposeSubscription();
    private bind();
    private unbind();
}
