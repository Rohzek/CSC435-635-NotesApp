import { SharedOptions } from './SharedOptions';
import { JsTools } from './../../../utilities/vanilla/jsTools';
import 'aureliatoolbelt-thirdparty/jquery.blockUI/jquery.blockUI.js';
import { IAutBlockUIOption } from './aut-block-ui-option';
export declare class JQueryBlockUI {
    private element;
    private option;
    private jsTools;
    private sharedOptions;
    settings: IAutBlockUIOption;
    block: boolean | string;
    blockPage: boolean | string;
    private content;
    private spinnerMessage;
    private elementId;
    private defaultOption;
    private allOptions;
    private id;
    constructor(element: Element, option: IAutBlockUIOption, jsTools: JsTools, sharedOptions: SharedOptions);
    private hasContent();
    private afterAttached();
    private setDefaultOption();
    private setSpinnerStyle(id, option);
    private blockChanged(isBlocked);
    private blockPageChanged(isBlocked);
    private getSizeUnit(text);
    private getSize(text);
    private detached();
}
