import { Uuid } from '../../../utilities/vanilla/uuid';
export declare class UuidCustomAttribute {
    private element;
    private idgeneratorV4;
    private id;
    constructor(element: Element, idgeneratorV4: Uuid);
    private bind();
}
