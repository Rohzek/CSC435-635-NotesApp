import { HttpClient } from 'aurelia-http-client';
export declare class MarkDownItCustomElement {
    private http;
    private isLoading;
    private myText;
    private editor;
    private preview;
    showToolBar: boolean;
    showPreview: boolean;
    showEditor: boolean;
    src: string;
    private slotContainer;
    constructor(http: HttpClient);
    private afterAttached();
    private srcChanged(newValue);
    private addText(text, cursorPosition?);
}
