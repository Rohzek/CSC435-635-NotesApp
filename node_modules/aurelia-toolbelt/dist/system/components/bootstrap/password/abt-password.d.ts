import { JsTools } from './../../../utilities/vanilla/jsTools';
export declare type ButtonColorType = 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'light' | 'dark';
export declare type ErrorDisplayType = 'none' | 'tooltip' | 'list';
export declare class PasswordCustomElement {
    private jsTools;
    inputClass: string;
    inputStyle: string;
    buttonClass: string;
    buttonStyle: string;
    buttonColorType: ButtonColorType;
    errorIcon: string;
    showPasswordIcon: string;
    hidePasswordIcon: string;
    progressBarHeight: string;
    errorDisplayType: ErrorDisplayType;
    showProgressBar: boolean | string;
    size: string;
    showPercent: boolean | string;
    passwordVisibility: boolean | string;
    text: string;
    scoreRange: object;
    requirements: object;
    passwordChanged: Function;
    private isInvisible;
    private txtPassword;
    private btnPassword;
    private iconPassword;
    private errorsList;
    private passwordTemplate;
    private progressBarValue;
    private percentValue;
    private progressBarClass;
    private progressBarColor;
    private passwordMeter;
    constructor(jsTools: JsTools);
    private afterAttached();
    private passwordVisibilityToggle();
    private generateErrorsAsHtml(errors);
    private getScoreRangeInfo(scoreRange);
    private getMinColorInfo(scoreRange);
    private getColorInfo(scoreRange, status);
    private textChanged(value);
}
