import { Uuid } from '../../../utilities/vanilla/uuid';
export declare class BootstrapNavItem {
    private uuid;
    class: string;
    style: string;
    href: string;
    title: string;
    active: boolean | string;
    disabled: boolean | string;
    private isFade;
    private tab_header;
    private tab_body;
    private element;
    constructor(element: Element, uuid: Uuid);
    private attached();
}
