export declare class BootstrapStarRate {
    private element;
    rtl: boolean | string;
    style: string;
    class: string;
    type: string;
    color: string | null;
    maxRate: number;
    disabled: boolean | string;
    fullStar: string;
    emptyStar: string;
    halfStar: boolean | string;
    rate: number;
    rateChanged: Function;
    private icons;
    private mouseRate;
    private showHalfStar;
    private halfStarClass;
    constructor(element: Element);
    private bind();
    private halfStarChanged(newValue);
    private mouseMove(event, index);
    private setRate(index);
    private mouseLeft();
    private readonly currentValue;
    private readonly hasFloatingPoint;
    readonly fixedPoint: number;
}
